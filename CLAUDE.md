# CLAUDE.md — 세꼼이(sekkomi.com) 작업 규칙

이 파일은 이 레포에서 일하는 모든 Claude 세션이 **가장 먼저** 읽는 규칙이다.

---

## ★ 최상위 원칙 — 정직

> **정직해라. 거짓 데이터는 아무 의미 없다. 어떤 경우에도 정직해라.**

이 원칙은 다른 모든 규칙, 목표, 마감, 편의보다 앞선다. 아래 규칙들과 충돌하면 **이 원칙이 이긴다.**

### 이 원칙이 실제로 요구하는 것

1. **모르는 것은 모른다고 쓴다.** "확인되지 않음", "미검증", "측정한 적 없음"은 실패가 아니라 정확한 보고다. 빈칸을 그럴듯한 추정으로 메우지 않는다.
2. **출처 없는 숫자를 쓰지 않는다.** 모든 수치에는 출처가 붙는다 — GSC/GA4 쿼리, Ahrefs 파일, 원문 URL 중 하나. 출처를 댈 수 없으면 그 문장을 지운다.
3. **내가 검증하지 않은 것은 검증했다고 말하지 않는다.** 서브에이전트·웹검색·문서가 준 숫자는 **그 출처를 명시**하고, 내가 원문 대조하지 않았으면 "미검증"이라고 붙인다. 서브에이전트의 자기 보고는 증거가 아니다.
4. **측정한 적 없는 것과 측정해서 0인 것을 구분한다.** 데이터가 없는 것은 "0"이 아니라 "미계측"이다. 이 둘을 섞으면 없는 결론이 생긴다.
5. **기대값과 대조하지 않은 0은 신호가 아니다.** "클릭 0", "노출 0"을 근거로 쓰려면 표본 크기와 기대값을 함께 제시한다. 기대값이 5건 미만이면 판단을 유보한다.
6. **"레포에 흔적이 없다"는 "안 했다"의 근거가 못 된다.** "레포에서는 확인되지 않음 — 콘솔 확인 필요"로 쓴다.
7. **"입력 완료"와 "동작 확인"을 구분한다.** 로그·알림으로 실제 동작을 본 뒤에만 ✅로 쓴다.
8. **틀렸으면 즉시, 눈에 띄게 정정한다.** 조용히 고치지 않는다. 이전 보고의 어느 문장이 왜 틀렸는지 명시한다.
9. **오너가 듣고 싶어할 답으로 기울지 않는다.** 오너가 제안한 방향이 데이터상 나쁘면 나쁘다고 쓴다. 동의는 근거가 있을 때만 한다.
10. **불리한 결과를 먼저 보고한다.** 좋은 소식 뒤에 숨기지 않는다.

### 과거에 이 원칙을 어긴 사례 (반복 금지)

| 날짜 | 무슨 일 | 무엇이 문제였나 |
|---|---|---|
| 2026-08-13 | "9~13위인데 클릭 0 → 제목·설명이 안 먹힌다" | 기대 클릭(3.7건)과 대조하지 않은 0을 신호로 읽었다. **폐기된 진단이다. 이 문장을 다시 쓰지 마라.** |
| 2026-08-11 | 발송대장의 낡은 표기를 그대로 믿고 틀린 보고 | 문서는 진실이 아니라 마지막으로 기록된 상태다. 시스템 실제 상태와 대조해야 했다 |
| 2026-08-05 | Netlify 환경변수를 "✅ 입력 완료"로 기재 | 배포가 없어 함수에 반영되지 않았다. 닷새간 문서와 실제가 어긋났다 |
| 2026-08-13 | 서브에이전트가 "정정 0회"라 보고 | 실제로는 5회 push였다. 자기 보고를 믿었다 |
| 2026-08-19 | "계산 완료 거의 0"을 전환 실패 근거로 반복 보고 | 신규방문 95명 대비 완료 8명(8.4%)이었다. 분모를 보지 않았다 |
| 2026-08-19 | 전략 논쟁 팩트베이스에 상속세·증여세 검색 볼륨을 넣지 않고 "상속주택 30"만 넣음 | **측정한 적 없는 시장을 측정된 것처럼 보이게 했다.** 그 위에서 나온 "피봇은 나쁜 안"이라는 결론은 근거가 부족하다 |

---

## 프로젝트 개요

- **세꼼이(sekkomi.com)** — 부동산 양도소득세 무료 계산기(채팅형 문답) + 블로그.
- 목표(2026-08-27 개정): **10만 명 목표 폐기.** 월 계산 완료·실사용자(봇·오너 제외) 증가가 지표다. 상세는 `claude/rules-amendment-2026-08-27.md` §1.
- 운영: 1인 + AI 자동화. 세무사 자격 없음 → **세무 자문 불가, 정보 제공만.**

## 단일 기준 문서

레포 밖 프로젝트 문서가 규칙의 단일 기준이다. 작업 전 반드시 읽는다.

| 문서 | 역할 |
|---|---|
| `claude/세꼼이-운영-규칙.md` | 무엇을/언제/누가 — **최우선 기준** |
| `claude/숏폼-제작-기준.md` | 릴스를 어떻게 만드는가 |
| `claude/아웃리치-발송대장.md` | 발송 이력·중복 발송 방지 원장 |
| `claude/세법-변동-점검-대장.md` | 세법·규제 변동 등재 원장 |
| `claude/Ahrefs-분석결과-2026-08-14.md` | 키워드 볼륨 실측 |
| 날짜별 실행 기록 | 이력 보관용, 규칙 아님 |
| `claude/rules-amendment-2026-08-27.md` | **2026-08-27 개정 — 운영 규칙 §1·§2·§5와 충돌하면 이 문서가 우선.** 릴스·콜드메일 중단, 주 2편(화 신규·목 강화), 네이버 기준, 단지 16편 noindex, 네이버 블로그 초안 생산 |

**이 CLAUDE.md의 최상위 원칙과 위 문서가 충돌하면 최상위 원칙이 이긴다.**

## 데이터 소스

- BigQuery `sekkomi-analytics-502000`
  - GA4 `analytics_544300402.events_YYYYMMDD` (asia-southeast3)
  - GSC `searchconsole.searchdata_*` (asia-northeast3)
  - **리전이 달라 조인 불가 — 따로 조회한다.**
- GA4 export 지연은 날마다 다르다. 매번 `MAX(_TABLE_SUFFIX)` 확인. **"어제 N건" 표현 금지**, "○월 ○일 기준 N건". 테이블이 아예 없는 날도 있다 → '테이블 없음'으로 명시.
- GSC 컬럼: `sum_position`은 `searchdata_url_impression`에만 있다. `searchdata_site_impression`에는 `sum_top_position`만 있다.
- **GSC와 Ahrefs가 어긋나면 GSC 우선.** Ahrefs는 GSC에 안 잡히는 영역을 보는 용도다.

## 레포 작업 규칙

- 읽기: `git clone --depth 1 https://github.com/MyungwooLEE/sekkomi.git <고유디렉터리>`
- 쓰기: GitHub MCP `push_files` 한 번의 커밋으로. **직접 `git push`는 403이다.**
- **`push_files` 호출 전 `files` 배열 원소 개수를 assert한다.** (2026-08-19에 4개 중 2개를 빠뜨려 커밋이 2개로 쪼개졌다)
- 커밋 접두어: 신규 글 `blog:` / 기존 글 강화 `boost:`
- 글 HTML 조립: `head = s[:s.index('<article>')+len('<article>')]`, `tail = s[s.rindex('</article>'):]`. 조립 후 `<article>`·`</article>` 각 1개 확인. **`assert OLD_SLUG not in out` 필수.**
- 조립 후 사용한 CSS 클래스가 `<style>`에 실제로 있는지 대조한다.
- 무결성 검증: `public/blog/*.html` 파일명 집합 vs sitemap vs index.html ItemList vs `href="/blog/(slug)"` **4집합 대조.** position 1~N 연속·중복 0. **단 `scripts/noindex-slugs.json`의 슬러그는 사이트맵 집합 비교에서 제외한다(2026-08-27, 의도된 불일치).**
- **description 함정**: `scripts/build-seo-meta.cjs`가 "슬러그가 `scripts/blog-descriptions.json`에 있고 AND 현재 HTML description이 80자 초과"일 때 덮어쓴다. → **80자 이내로 쓰면 맵 주입이 멈춘다.** 큰따옴표 금지.
- `_parts` 수정 시 `_parts/index.sha256`도 갱신. featured 블록·`.feat-cover` CSS·EP.01 보존 카드는 손대지 않는다.
- Netlify 빌드 스킵 규칙: `public·netlify·netlify.toml·scripts·_parts·package.json` 변경이 있을 때만 빌드한다. `claude/`·`tools/` 문서만 바꾼 커밋은 배포되지 않는다(의도된 동작).
- 환경변수를 바꿨으면 반드시 `Clear cache and deploy site`. 로그에 `Building without cache`와 `N new function(s) to upload`가 찍혀야 반영된 것이다.

## 서브에이전트 위임 규칙

**서브에이전트는 한글을 깨뜨린다. 가설이 아니라 실측이다.** (`숏폼`→`숫폼`, `표2`→`푒`, `아낄까`→`아낀까`)

1. push 위임 시 **경로 + sha256 + "새 클론으로 sha256 재검증, 불일치 시 최대 2회 정정" + "files 배열 원소 N개 assert"**를 프롬프트에 명시한다.
2. **서브에이전트의 자기 보고를 믿지 않는다.** 본세션이 별도 클론을 떠서 sha256을 직접 계산한다.
3. 서브에이전트가 웹에서 가져온 숫자는 **출처 URL과 함께 "미검증"으로 표시**한다. 본세션이 원문 대조한 것만 검증된 것이다.
4. 리드 수집·세법 점검 같은 무거운 작업은 본세션이 글을 쓰는 동안 병렬로 돌린다.

## 도구 사용

- **어떤 도구든 거부되거나 실패하면 즉시 포기하고 다음 단계로. 재시도하지 않는다.** (2026-08-03에 Resend `list-emails` 8회 반복 호출로 4시간을 날렸다. 문제는 도구가 아니라 반복 호출이었다)
- Resend `list-emails`는 **1회만** 호출한다. 상세가 필요하면 `get-email`을 최대 2회까지.
- `WebFetch`는 1회 시도 후 실패하면 '미검증'으로 남기고 진행. **curl·wget·python 등으로 우회하지 않는다**(환경 차단).
- Bash의 `grep`이 분류기에 막히면 `Grep` 도구를 쓴다(절대경로 필요).
- 예약 실행 세션에서는 `mcp__remote-devices__*`를 쓰지 않는다. 오너 컴퓨터는 꺼져 있다고 가정한다.

## 소재 선정 규칙

- **1차 기준은 검색 볼륨, 2차 기준이 레포 공백이다.** "우리가 안 쓴 것"과 "사람들이 찾는 것"은 다르다.
  - 2026-08-14 사례: 입주권을 "언급 15편·단독 0편"이라는 이유로 1순위에 뒀으나 볼륨 20으로 최하위였고, 편성에 없던 소형·저가주택이 290이었다.
- **볼륨 0인 소재는 쓰지 않는다.** 단지명 키워드(은마·압구정·잠실주공5·올팍·여의도 시범)는 전부 볼륨 0으로 확인됐고 17편이 4주 노출 0이었다. **다시 하지 마라.**
- 뉴스 브리핑형은 19편 4주 노출 3(편당 0.16)이다. 검색 자산이 아니다. **(구글 기준. 네이버에선 정책 설명형 1편이 30일 노출의 47% — 개정 문서 §3)**
- 신규 글은 본문 내부링크 최소 3개. 조립 스크립트에 `assert`로 강제한다.
